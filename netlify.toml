[template.environment]
  NOTION_TOKEN = "Your Notion Integration Token"
  # Single Notion database used for both posts and pages.
  # Posts: entries where Type property is "post" or unset.
  # Pages: entries where Type property is "page".
  NOTION_DATASOURCE_ID = "Your Notion Data Source ID"
