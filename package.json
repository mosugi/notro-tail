{
  "name": "root",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mosugi/notro.git"
  },
  "scripts": {
    "dev": "pnpm --filter notro-blog run dev",
    "build": "pnpm --filter notro-blog run build",
    "preview": "pnpm --filter notro-blog run preview",
    "docs:dev": "pnpm --filter notro-docs run dev",
    "docs:build": "pnpm --filter notro-docs run build",
    "blank:dev": "pnpm --filter notro-blank run dev",
    "blank:build": "pnpm --filter notro-blank run build",
    "gallery:dev": "pnpm --filter notro-gallery run dev",
    "gallery:build": "pnpm --filter notro-gallery run build",
    "test": "pnpm -r run test",
    "format": "pnpm -r run format",
    "build:packages": "pnpm --filter satteri-beautiful-mermaid --filter create-notro --filter notro-md-sync run build",
    "changeset-version": "changeset version",
    "changeset-publish": "pnpm run build:packages && changeset publish",
    "reflect": "notro-md-sync publish content/docs --db ${NOTION_DATASOURCE_ID_DOCS:-$NOTION_DATASOURCE_ID}",
    "reflect:blog": "notro-md-sync publish content/blog --db ${NOTION_DATASOURCE_ID_BLOG:-$NOTION_DATASOURCE_ID}"
  },
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=9.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "sharp"],
    "overrides": {
      "notro-loader": "workspace:*",
      "satteri-beautiful-mermaid": "workspace:*"
    }
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.2",
    "@changesets/cli": "^2.29.8",
    "notro-md-sync": "workspace:*",
    "prettier": "^3.8.1",
    "prettier-plugin-astro": "^0.14.1"
  }
}
