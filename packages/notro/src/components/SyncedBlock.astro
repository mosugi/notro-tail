---
import type { SyncedBlockBlockObjectResponseType } from "../loader/schema";
interface Props {
  block: SyncedBlockBlockObjectResponseType;
}

const { block } = Astro.props;
---

<div id={block.id} class=`nt-selectable nt-transclusion_reference-block`>
  <slot />
</div>
