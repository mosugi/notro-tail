---
// Component mapping target for <notion-columns> custom elements in Notion markdown content.
interface Props {
  [key: string]: unknown;
}

const { ...rest } = Astro.props;
---

<div class="my-4 flex gap-6" {...rest}>
  <slot />
</div>
