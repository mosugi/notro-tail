---
// Component mapping target for <blockquote> elements in Notion markdown content.
interface Props {
  class?: string;
  [key: string]: unknown;
}

const { class: className, ...rest } = Astro.props;
---

<blockquote class:list={["nt-blockquote", className]} {...rest}>
  <slot />
</blockquote>
